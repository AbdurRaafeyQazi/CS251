#include "lab1.h"

#include <iostream>
#include <string>

using namespace std;

int numWordsIn(const string& sentence) { return 0; }

int main() {
  // This section gets a from standard input. We will talk about input on
  // Friday.
  string input_line;
  getline(cin, input_line);

  // TODO student: print the number of words in input_line
  

  return 0;
}